// src/pages/Home.tsx

import { Card, Typography } from 'antd';

export default function Home() {
  return (
    <Card>
      <Typography.Title>Home</Typography.Title>
      <Typography.Paragraph>Welcome — this is a Vite + React + TS + AntD starter.</Typography.Paragraph>
    </Card>
  );
}
