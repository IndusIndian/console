// src/pages/Home.tsx

import { Card, Typography } from 'antd';

export default function Home() {
  return (
    <Card>
      <Typography.Title>Welcome — BT Trading & Command</Typography.Title>
    </Card>
  );
}
